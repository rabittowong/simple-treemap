import { Routes } from '@angular/router';

import { TreemapContentComponent } from './treemap-content.component';

export const routes: Routes = [
  { path: '', component: TreemapContentComponent },
];
